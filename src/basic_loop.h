#pragma once

// Ponto de entrada do interpretador BASIC (equivalente ao void loop() do
// .ino original). Implementado em basic_loop.cpp, chamado a partir do
// loop() do Arduino em main.cpp.
void basic_run();
